 # Arrays
  --Prefix sum
  --Trap rain water problem
  --maximum j-i such that arr[j]>arr[i]
  --majority element (Moores voting algorithm)
  --Array intersection and union using hashset
 2D Matrix
 --Transpose of Matrix
 --rotateMatrix 90 deg clockwise
 --search in matrix sorted in rowwise and columnwise 

# Recursion
 --basic
 --power calcuation 
 -- No. of path in n*m matrix
 --subsequence of string
 --stair path for maximum three jump
 --keypad combination

 # Mathematics
 --GCD
 --LCM
 --Trailing zero in factorial
 # Bit Manipulation
 -- RightMost set bit
 -- Kernighans algrorithm
 --singleNumber which is appear only once in array of repeating element
 --two number which appear only once
 --duplicate and missing number
 #string
 --palindromic substring
 --String compression 1 and 2
 --substring
 #Hashmap and hashset
 --most  repeated character
 --hashset basic
 --consquective longest subarray
 --largest subarray with zero sum
 --No. of subarray with zero sum
 